{
    "title": "Used RAM Memory",
    "type": "percent",
    "params": {
        "query": "((node_memory_MemTotal_bytes{{instance='{0}',job='{1}'}} - node_memory_MemFree_bytes{{instance='{0}',job='{1}'}}) / (node_memory_MemTotal_bytes{{instance='{0}',job='{1}'}} )) * 100",
        "description": "Non available RAM memory"
    },
    "height": 3
}
