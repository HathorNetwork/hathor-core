N_TXS=2000
N_BLOCKS=1
N_RUNS=3
INPUTS=1,2,4,8,16
# Outputs per tx. Empty = match inputs (the diagonal sweep). Set a list (e.g. 1,2,4,8,16)
# to sweep the full inputs x outputs matrix.
OUTPUTS=
BENCH_FILE=tps_results.json
EXPECTED_NPROC=4
